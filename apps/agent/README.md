# MediaBridge Agent

This directory is reserved for the local MediaBridge Agent. The Agent should
remain independently packageable and use `packages/api-contracts` for API
communication with the server.
