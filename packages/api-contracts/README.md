# API contracts

Place the OpenAPI source, generated TypeScript clients, and shared request /
response contracts here. The server remains the source of truth for the API;
Web, extension, and Agent clients should consume generated artifacts.
